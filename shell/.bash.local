#!/bin/bash


# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# Node Version Manager

export NVM_DIR="/home/ab/.nvm"

[ -f "$NVM_DIR/nvm.sh" ] \
    && . "$NVM_DIR/nvm.sh"

[ -f "$NVM_DIR/bash_completion" ] \
    && . "$NVM_DIR/bash_completion"
